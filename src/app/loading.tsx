export default function Loading() {
  return (
    <main className="center-screen">
      <section className="glass-panel access-panel">
        <p className="eyebrow">FairCroft CoreOne</p>
        <h1>Loading government services...</h1>
        <p>Preparing the civilian PDA, CAD/MDT, dispatch command center, and live records system.</p>
      </section>
    </main>
  );
}
